<UserControl x:Class="PasswordManager.Views.AllPasswordsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PasswordManager.Views"
             xmlns:viewmodels="clr-namespace:PasswordManager.ViewModels"
             xmlns:customcontrols="clr-namespace:PasswordManager.CustomControls"
             d:DataContext="{d:DesignInstance Type=viewmodels:AllPasswordsViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="1200">



    <StackPanel>

        <customcontrols:PasswordSearch Grid.Row="0"
                                       SearchCriteria="{Binding SearchFilter, Mode=TwoWay}"
                                       Height="40"
                                        HorizontalAlignment="Stretch"
                                       />

        <customcontrols:PasswordDataGrid PasswordList="{Binding Passwords}"
                                         Grid.Row="1" HorizontalAlignment="Stretch"/>


    </StackPanel>
</UserControl>
