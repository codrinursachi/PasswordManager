<Window x:Class="PasswordManager.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordManager.Views" xmlns:viewModel="clr-namespace:PasswordManager.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="1600"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type viewModel:AllPasswordsViewModel}">
            <local:AllPasswordsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:FavoritesViewModel}">
            <local:FavoritesView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:TagsViewModel}">
            <local:TagsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewModel:CategoryViewModel}">
            <local:DirectoryView/>
        </DataTemplate>
    </Window.Resources>

    <Border CornerRadius="10"
            BorderThickness="1">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{StaticResource winBorderColor1}" Offset="0"/>
                <GradientStop Color="{StaticResource winBorderColor2}" Offset="0.75"/>
                <GradientStop Color="{StaticResource winBorderColor3}" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border CornerRadius="10,0,0,10"
                    Grid.Column="0">

                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0.7">
                        <GradientStop Color="{StaticResource secondaryBackColor1}" Offset="0"/>
                        <GradientStop Color="{StaticResource secondaryBackColor2}" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <StackPanel>

                    <StackPanel Orientation="Horizontal"
                                Height="35"
                                Margin="15,25,0,40">

                        <Border CornerRadius="15" Height="30" Width="30">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{StaticResource winBorderColor1}" Offset="0"/>
                                    <GradientStop Color="{StaticResource winBorderColor2}" Offset="0.75"/>
                                    <GradientStop Color="{StaticResource winBorderColor3}" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>

                            <Border CornerRadius="15" Height="30" Width="30">
                                <Border.Background>

                                    <ImageBrush ImageSource="/Images/Logo.png"
                                                Opacity="0.2"/>

                                </Border.Background>

                            </Border>
                        </Border>

                        <TextBlock Text="Password Manager"
                                   Foreground="{StaticResource titleColor2}"
                                   FontSize="20"
                                   FontFamily="Montserrat"
                                   FontWeight="Medium"
                                   VerticalAlignment="Center"
                                   Margin="10,0,0,0"/>

                    </StackPanel>

                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color1}"
                                 Command="{Binding ShowAllPasswordsView}"
                                 IsChecked="True">

                        <TextBlock Text="All Passwords" Style="{StaticResource menuButtonText}"/>

                    </RadioButton>

                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color2}"
                                 Command="{Binding ShowFavoritesView}">

                        <TextBlock Text="Favorites" Style="{StaticResource menuButtonText}"/>

                    </RadioButton>

                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color3}"
                                 Command="{Binding ShowLabelsView}">

                        <TextBlock Text="Tags" Style="{StaticResource menuButtonText}"/>

                    </RadioButton>

                    <RadioButton Style="{StaticResource menuButton}"
                                 Tag="{StaticResource color4}"
                                 Command="{Binding ShowCategoryView}">

                        <TextBlock Text="Categories" Style="{StaticResource menuButtonText}"/>

                    </RadioButton>

                </StackPanel>

            </Border>

            <Border CornerRadius="0,10,10,0"
                    Grid.Column="1">

                <Border.Background>
                    <LinearGradientBrush StartPoint="1,1" EndPoint="0,0">
                        <GradientStop Color="{StaticResource primaryBackColor1}" Offset="0"/>
                        <GradientStop Color="{StaticResource primaryBackColor2}" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="55"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel x:Name="pnlControlBar"
                            Grid.Row="0"
                            Orientation="Horizontal"
                            FlowDirection="RightToLeft"
                            Background="Transparent"
                            Margin="0,0,5,0"
                            MouseLeftButtonDown="pnlControlBar_MouseLeftButtonDown"
                            MouseEnter="pnlControlBar_MouseEnter">

                        <Button x:Name="btnClose"
                                Style="{StaticResource controlButton}"
                                Tag="{StaticResource color4}"
                                Content="X"
                                Click="btnClose_Click">

                        </Button>

                        <Button x:Name="btnMaximize"
                                Style="{StaticResource controlButton}"
                                Tag="{StaticResource color6}"
                                Content="▢"
                                Click="btnMaximize_Click">

                        </Button>

                        <Button x:Name="btnMinimize"
                                Style="{StaticResource controlButton}"
                                Tag="{StaticResource color8}"
                                Content="_"
                                Click="btnMinimize_Click">

                        </Button>

                    </StackPanel>
                    <Grid Grid.Row="1" VerticalAlignment="Bottom">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Caption}"
                                   Margin="25,0,0,0"
                                   Grid.Column="0"
                                   Foreground="{StaticResource titleColor2}"
                                   FontSize="16"
                                   FontFamily="Montserrat"
                                   FontWeight="Medium"
                                   VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right">

                            <ComboBox x:Name="dtbSelector" ItemsSource="{Binding Databases, UpdateSourceTrigger=PropertyChanged}" SelectedIndex="{Binding SelectedDb}" MouseLeftButtonUp="dtbSelector_MouseLeftButtonDown"/>
                            <Button x:Name="btnAddPass"
        Style="{StaticResource actionButton}"
        Margin="0,0,25,0"
        Click="btnAddPass_Click">
                                <TextBlock Text="+ Add Password"
               Foreground="{StaticResource titleColor2}"
               FontSize="16"
               FontFamily="Montserrat"
               FontWeight="Medium"/>
                            </Button>



                        </StackPanel>


                    </Grid>

                    <ContentControl x:Name="childView" Content="{Binding CurrentChildView}"
                                    Grid.Row="2"
                                    Margin="25"/>

                </Grid>

            </Border>

        </Grid>

    </Border>

</Window>
