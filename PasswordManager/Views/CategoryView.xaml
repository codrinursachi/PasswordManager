<UserControl x:Class="PasswordManager.Views.DirectoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PasswordManager.Views"
             xmlns:viewmodels="clr-namespace:PasswordManager.ViewModels"
             xmlns:customcontrols="clr-namespace:PasswordManager.CustomControls"
             d:DataContext="{d:DesignInstance Type=viewmodels:CategoryViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TreeView x:Name="Categories"
                  ItemsSource="{Binding Categories}"
                  Height="auto"
                  Width="auto"
                  SelectedItemChanged="Categories_SelectedItemChanged"
                  Background="Transparent">

            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <TextBlock x:Name="categoryItem"
                               Text="{Binding Name}"
                               Foreground="White"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <customcontrols:PasswordListView PasswordList="{Binding Passwords}"
                                         Grid.Column="1"/>
    </Grid>
</UserControl>
